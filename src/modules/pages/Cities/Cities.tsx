import React from 'react'

import useStyles from './styles'

const Cities: React.FC = () => {
  const c = useStyles()
  return (
    <div>
      <h1>Cities</h1>
    </div>
  )
}

export default Cities
